
docker compose up --build -d



docker build -t scom-app:latest .

docker run -d \
  --restart unless-stopped \
  -p 8050:8050 \
  obilab/scom-app:latest


1) Install docker 2) run this: docker pull obilab/scom-app:latest (it will take time wait untill complete) 3) thrn run this:  docker run -p 8052:8050 obilab/scom-app:latest 4) see on broser: http://localhost:8050/scom/